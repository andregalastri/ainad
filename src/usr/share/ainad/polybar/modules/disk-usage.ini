[module/disk-usage]
type = internal/fs

mount-0 = "/"

interval = 10

fixed-values = true

format-mounted-prefix = ""
format-unmounted-prefix = ""

label-mounted = " %free%"
label-unmounted = "%mountpoint%: NM"
